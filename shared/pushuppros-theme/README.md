# PushupPros shared theme

CSS consumed by **`pushuppros-marketing`** and **`pushuppros-app`**.

Each app’s `src/index.css` imports:

```css
@import '../../shared/pushuppros-theme/theme.css';
```

Edit tokens and components under this folder so both apps stay visually aligned.
