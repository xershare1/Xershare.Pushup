services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pushup
    ports:
      - "5432:5432"
    volumes:
      - pushup_pgdata:/var/lib/postgresql/data

volumes:
  pushup_pgdata:
