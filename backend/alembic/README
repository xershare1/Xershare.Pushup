Generic single-database configuration.

If `alembic upgrade` fails writing a long revision id to `alembic_version.version_num` (VARCHAR 32 default), run once from the backend directory: `python scripts/fix_alembic_version_num.py`, then upgrade again. SQL fallback: `scripts/fix_alembic_version_num.sql`. See ../README.md.