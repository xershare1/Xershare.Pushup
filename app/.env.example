# Copy to .env.development or configure in your host (e.g. Netlify/Vercel env UI).

# "true" = in-browser mock (no server). Omit or set false to call FastAPI.
VITE_USE_MOCK_API=true

# Required when VITE_USE_MOCK_API is not true
VITE_API_BASE_URL=http://localhost:8000
